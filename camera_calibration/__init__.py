"""ZED + ChArUco camera / hand-eye calibration toolkit.

This package is self-contained inside camera_calibration/ and does not
modify anything elsewhere in the repository. It never connects to a camera
or robot on import — all hardware access is opt-in inside scripts/.
"""
