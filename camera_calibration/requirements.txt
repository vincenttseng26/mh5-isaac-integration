# Exactly ONE opencv contrib package — see README "Known OpenCV issues".
# Do NOT also have opencv-python / opencv-python-headless / *-headless
# installed in the same environment; that combination is a common cause of
# native segfaults inside cv2.aruco.
opencv-contrib-python>=4.8,<5

numpy>=1.23
pyyaml>=6.0
matplotlib>=3.5   # only needed for the print-locked PDF board export
pytest>=7.0

# Optional, only if you actually have a ZED camera + ZED SDK installed:
# pyzed (install per stereolabs.com instructions, matched to your ZED SDK version)
